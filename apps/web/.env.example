# Generate each value independently with a cryptographically secure random source.
# Both values are Base64-encoded 32-byte keys. Never commit real values.
AI_SECRET_MASTER_KEY=
AUTH_SECRET_MASTER_KEY=
# Comma-separated ChatGPT account emails allowed to claim the first administrator.
# Required outside localhost; keep the Sites access policy restricted during setup.
ADMIN_BOOTSTRAP_EMAILS=
